Good progress on both fronts — but let's not wave this through yet, there's still one soft spot worth pressing on before Run B.

**Step 1.1 — the sign-flip conditioning is now correctly implemented, and that's a real, physically meaningful result.** Confirming the label genuinely flips across `sign(cos ι)` (combo_B favored at `ι=π/4`, combo_A at `ι=3π/4`) matches exactly what Sec. 6 of the original doc flagged as an open question, and the recommendation to condition the label on sign at training time rather than hardcoding one is the right call.

**But the ratio itself — 1.2x in both halves — is still weak, and splitting by sign didn't change that number at all.** That's worth sitting with for a second: my hypothesis last time was that mixing both sign regimes together was *diluting* a stronger underlying signal. That hypothesis predicted splitting would reveal a much cleaner ratio in each half. It didn't — same 1.2x on both sides. That doesn't mean something's broken, but it does rule out my mixing-dilution explanation, so the weak ratio needs a different account before you trust it as the real physics rather than a harness limitation. Two things worth checking before moving on:

1. **You tested at ι=π/4 and ι=3π/4 — both moderately inclined, neither near the extremes.** Physically, the well-constrained/poorly-constrained gap should *widen* as ι→0 and *close* as ι→π/2 — you're not testing near either boundary, you're testing near the middle, where a modest ratio is actually what theory predicts anyway. A 1.2x ratio at 45° isn't necessarily alarming; it would be alarming if the ratio *stayed* at 1.2x as you swept closer to ι=0, where it should grow sharply. Worth extending the sweep — a handful of points from ι=0.1 to ι≈1.4, not just the two quarter/three-quarter points — to confirm the ratio actually trends the way the physics demands rather than sitting flat.
2. **Check whether 1.2x is even statistically distinguishable from 1.0x (no real difference) at whatever sample size the harness used.** A bootstrap or permutation-test confidence interval on that ratio would settle whether this is a real, if modest, effect or noise you can't yet tell apart from "no signal."

**Step 1.2 — the fix (switching to linear interpolation on `cos²ι`) is the right move, and the numbers are now internally consistent** (0 at face-on, 0.1212 at edge-on, monotonic, no more sign-flipped nonsense from the bad polynomial). Two things to confirm before trusting it, though:

1. **A residual of exactly 0.00 here is expected by construction, not evidence of a good fit** — linear interpolation passes exactly through whatever points it was built from, so this residual number can't tell you whether the curve is well-behaved *between* those points. Worth explicitly reporting how many `ι` values were swept to build this (if it's just the two reported endpoints, you have a straight line between two numbers, not a validated curve) and plotting the intermediate shape to confirm it's smooth and monotonic rather than just checking the two endpoints printed.
2. **Confirm the sky-averaging requirement from Step 1.2 actually ran** (averaging the Jacobian/condition-number over multiple sky positions before fitting `w`, per the DeepSeek-review addition) — the printout doesn't show evidence either way, and it's easy for this to have been silently skipped if the harness defaults to a single toy `(a,b)` pair.

**Bottom line:** Step 1.6 is solid, proceed on that front without concern. Step 1.1's direction (sign-flip, conditional labeling) is right, but the ratio's weakness needs one more check (sweep closer to ι=0, confirm the trend, run a quick significance check) before you trust it enough to derive a curriculum weight from it. Step 1.2's fix is a real improvement over the broken polynomial, but confirm it's built from more than two sample points and that sky-averaging was actually in the loop before this `w(ι)` goes anywhere near Run B.
