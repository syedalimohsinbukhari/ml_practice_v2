% On the Recoverability of Coalescence Phase and Polarization Angle from Gravitational-Wave Strain: A Systematically Verified Null Result
%
% Standalone LaTeX source for arXiv submission.
% Self-contained: no external class or preamble is required beyond the packages loaded below, all of which are present
% in arXiv's TeX Live distribution.
%
% Adapted from the thesis chapter of the same title (experiments/phic_psi_poc/thesis/chapter_phic_psi_degeneracy.tex) on
% branch poc/phic-psi-degeneracy.
% Content and structure are carried over essentially unchanged (a "light trim"): only thesis-internal scaffolding
% (draft-status banner, adversarial-review bookkeeping, cross-references to "the wider thesis") has been removed, and
% paper front matter (title, author, abstract, a resolved bibliography) has been added.
% All quantitative values are taken verbatim from the experiment record; the claim-to-artifact map in the Appendix
% traces every number to a versioned file in the accompanying repository.
%
% Figures are referenced from the experiment output directories one level up (see \graphicspath below).
% Before an actual arXiv upload, gather the referenced PNG files into the submission directory alongside this .tex
% (arXiv compiles from a flat/self-contained source tree) -- they are left in place here rather than duplicated into paper/.

\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{graphicx}
\usepackage{amssymb}
\usepackage{amsfonts}
\usepackage{booktabs}
\usepackage{amsopn}
\usepackage{amsmath}
\usepackage{amstext}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}
\usepackage[numbers,sort&compress]{natbib}
\usepackage{siunitx}
\DeclareSIUnit\solarmass{M_{\odot}}

\graphicspath{{../}{../diagnostic_output/}{../analysis_output/}{../lam0_ablation_output/}{../lam005_retune_output/}{../lam010_retune_output/}}

\title{On the Recoverability of Coalescence Phase and Polarization Angle from Gravitational-Wave Strain: A Systematically Verified Null Result}
\author{Syed Ali Mohsin Bukhari\thanks{Affiliation and correspondence email to be added before submission.}}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
The coalescence phase $\varphi_c$ and polarization angle $\psi$ of a compact-binary gravitational-wave signal are, for the dominant quadrupole radiation mode, degenerate with each other in the face-on limit: only a single combination, effectively $\varphi_c \pm 2\psi$, is constrained by the strain, and the individual angles disentangle only as the system moves toward edge-on.
We test directly whether a point-estimating deep neural network can recover $\varphi_c$ and $\psi$ from two-detector strain – individually or through their better-conditioned sum/difference combination – across five trunk architectures, two head parameterization, and four magnitude-regularization strengths, roughly nine full training campaigns in all.
The answer is a defended null: every configuration converges to the random-guessing baseline on both angles, while the same networks recover chirp mass, merger time, signal-to-noise ratio, and sky position with high fidelity from the same shared representation.
The null survives a verification battery --- configuration and code-path audits, a 10{,}000-permutation label-shuffle bootstrap, SNR- and inclination-stratified analysis, a regularization ablation, and a pre-registered two-arm sweep with mechanically evaluated success criteria – and is corroborated by an analytic prerequisite study that independently bounds how little conditioning structure the combination carries ($\approx 1.16\times$ on average, vanishing face-on).
Two of the seven configurations reach a fully certified interpretability status; the remainder corroborate the same result under progressively relaxed certification.
This null does not reopen a settled question: neural-posterior-estimation methods already represent this degeneracy correctly by construction, since a full posterior need not commit to a single value for an unidentifiable target.
What point estimation exposes instead is a practical hazard for the point-estimate and hybrid pipelines still built on regression heads of this kind: the failure is not obvious, and for a significant stretch of this investigation it mimicked partial success.
Beyond the physics result, we report the diagnostic discipline that made the null trustworthy: two genuine, initially invisible optimization pathologies --- a saturating output activation and a norm-blind loss that let raw output magnitude drift unboundedly --- had to be found and mechanistically understood before flat loss curves could be read as evidence about the underlying physics rather than as engineering failure, and every subsequent metric was validated at the mechanism level before being trusted.
\end{abstract}

\tableofcontents

\section{Introduction}
\label{sec:phicpsi:intro}
\input{introduction}

\section{Problem Formulation}
\label{sec:phicpsi:problem}
\input{problem-formulation}

\section{Analytic Prerequisite Study}
\label{sec:phicpsi:prereq}

Before any training, the combination-space hypothesis was tested analytically on the dataset itself, using a toy detector-response model: for a sweep of inclinations, we computed the correlation between strain-derived quantities and each candidate combination, $A = \varphi_c + 2\psi$ and $B = \varphi_c - 2\psi$, and formed the ratio of the better-correlated (``well-constrained'') to the worse-correlated combination.

The definitive 100-point inclination sweep (50 points per sign of $\cos\iota$, 200 sky positions, 5{,}000-draw bootstrap) gave an unambiguous, sign-symmetric structure (Fig.~\ref{fig:phicpsi:sweep}):
\begin{itemize}
\item For $\cos\iota > 0$, combo $B$ is the better-constrained combination at \textbf{50 of 50} inclination points; for $\cos\iota < 0$, combo $A$ wins 50 of 50 --- a clean sign flip, as the circular-polarization picture predicts;
\item The preference ratio is strongest face-on, $\approx 1.56\times$ decaying monotonically toward $\approx 1.05$--$1.08\times$ at edge-on, never crossing 1.0 (minimum 1.049 at $\iota = 1.62$);
\item Population-bootstrapped mean ratios are $1.155\times$ (95\% CI $[1.118, 1.195]$) for $\cos\iota > 0$ and $1.171\times$ (95\% CI $[1.130, 1.216]$) for $\cos\iota < 0$ – modest but statistically unambiguous.
\end{itemize}

\begin{figure}[tbp]
    \centering
    \includegraphics[width=\textwidth]{sweep_1_1_ratio_vs_iota}
    \caption[
        Analytic check of the $\varphi_c$--$\psi$ degeneracy structure
    ]{
        Analytic check of the $\varphi_c$--$\psi$ degeneracy structure: ratio of the strain correlation with the well-constrained combination to the poorly constrained one, as a function of inclination, for $\cos\iota > 0$ (left; combo $B = \varphi_c - 2\psi$ favored) and $\cos\iota < 0$ (right; combo $A = \varphi_c + 2\psi$ favored).
        Face-on systems prefer the expected combination by $\approx 1.56\times$; the preference shrinks to $\approx 1.05$--$1.08\times$ at edge-on but never inverts.
        Bootstrap means: $1.155\times$ (95\% CI $[1.118, 1.195]$) and $1.171\times$ (CI $[1.130, 1.216]$).
    }
    \label{fig:phicpsi:sweep}
\end{figure}

The same study produced the curriculum weight: a Jacobian-conditioning derivation (finite-difference Jacobian of the angle-to-response map, condition number averaged over sky positions, interpolated on $\cos^2\iota$) yielded an empirical weight with $w(\text{face-on}) = 0.0000$ and $w(\text{edge-on}) = 0.1212$ after an earlier polynomial fit was rejected for producing negative weights (maximum residual 1.32, $w(\text{face-on}) = -5.29$ from the fit against 0.000 from the raw data).
The trained runs use the analytically motivated fallback
\begin{equation}
    w(\iota) = 1 - \cos^2\iota = \sin^2\iota,
    \label{eq:curriculum}
\end{equation}
which shares the fitted weight's essential feature (zero weight where the degeneracy is exact, maximal weight edge-on) while avoiding the fit's pathologies; the fitted variant was derived and validated but never wired into training, a warning noted in \S\ref{sec:phicpsi:threats}.
The population balance was also confirmed: 28.7\% of the samples are near-face-on ($|\cos\iota| > 0.9$) and 32.7\% near-edge-on ($|\cos\iota| < 0.5$), so neither regime is vanishingly rare.

The prerequisite study's gate verdict was GO: the combination structure exists in the data, with the predicted sign behavior.
Whatever the networks subsequently failed to learn, they did not fail for want of an analytically real target.


\section{Methods}
\label{sec:phicpsi:methods}
\input{methods}

\section{The Diagnostic Arc: Making the Measurement Trustworthy}
\label{sec:phicpsi:arc}
\input{the-diagnostic-arc-making-the-measurement-trustworthy}

\section{Results: A Defended Null}
\label{sec:phicpsi:results}
\input{results-a-defended-null}

\section{The Pre-Registered $\lambda$ Retune}
\label{sec:phicpsi:prereg}
\input{the-pre-registered-$lambda$-retune}

\section{Discussion}
\label{sec:phicpsi:discussion}
\input{discussion}

\section{Conclusion}
\label{sec:phicpsi:conclusion}

We set out to determine whether the coalescence phase and polarization angle of compact-binary signals are recoverable from strain by direct neural regression, individually or in their physically motivated sum/difference combinations.
After eliminating two genuine optimization pathologies that initially masqueraded as (and then obscured) the answer --- $\tanh$ saturation at initialization, and magnitude drift induced by a norm-blind circular loss --- and after subjecting the resulting flat learning curves to a verification battery spanning configuration audits, permutation tests, population stratification, an ablation, and a pre-registered two-arm regularization sweep, the answer is: \textbf{no}.
Every configuration tested, at every capacity and every regularization strength, converged to the optimal constant predictor, at the analytic random-guessing error, while learning four other parameters well from the same shared representation – and no result at any point cleared even the first gate of a pre-declared path to counter-evidence.
The fully certified base of that claim is two $\lambda$-matched configurations (\S\ref{sec:phicpsi:pool}); the wider table of seven architectures corroborates without independently repeating the same test.
The primary evidence is the label-permutation bootstrap (\S\ref{sec:phicpsi:bootstrap}), the SNR stratification (\S\ref{sec:phicpsi:snr}), and the inclination stratification (\S\ref{sec:phicpsi:inclination}), all pre-specified or run against fixed, pre-existing checkpoints; the perturbation trace (\S\ref{sec:phicpsi:asymmetry}) is corroborating mechanistic detail, not primary evidence, since its closure rests on a channel adopted after its pre-registered instrument failed calibration – the degeneracy verdict does not depend on it.
Because that channel was adopted post hoc, the trace cannot independently close any confounding on its own; it corroborates a verdict established elsewhere, pending the fresh-holdout replication that would let A.3's closure stand on its own pre-registered footing.
The degeneracy is effectively exact for this population as a point-estimation problem.
The value of this paper lies as much in the demonstrated discipline --- mechanism before metrics, code-validated controls, pre-registered criteria inside the engineering loop – as in the null it certifies, and both carry directly into the inclination-conditioned and posterior-based formulations that follow.


\section*{Appendix: Claim-to-artifact map}
\label{sec:phicpsi:appendix}
\input{appendix-claim-to-artifact-map}

\end{document}
